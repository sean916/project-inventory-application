extends layout

block content
    h1= title

    ul
        each val in iteminstance_list
            li
                a(href=val.url) #{val.item.name}
                |   - Located at 
                a(href=val.store_location.url) #{val.store_location.detail} 
        
        else
            li There are no item instances in this database.